<div class="row">
  <div class="input-field col s12">
    <%= materialized_form_field(f, :text_field, :name) %>
    <%= f.label :name %>
    <%= display_error(f.object, :name) %>
  </div>
</div>

<div class="row">
  <div class="input-field col s12">
    <%= materialized_form_field(f, :email_field, :email) %>
    <%= f.label :email %>
    <%= display_error(f.object, :email) %>
  </div>
</div>

<div class="row">
  <div class="input-field col s12">
    <%= materialized_form_field(f, :password_field, :password) %>
    <%= f.label :password %>
    <%= display_error(f.object, :password) %>
  </div>
</div>

<div class="row">
  <div class="input-field col s12">
    <%= materialized_form_field(f, :password_field, :password_confirmation) %>
    <%= f.label :password_confirmation %>
    <%= display_error(f.object, :password_confirmation) %>
  </div>
</div>
